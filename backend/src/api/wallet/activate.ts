// Placeholder: API Endpoint to Generate Sponsored `createAccount` XDR
// Will call transaction_builder service to generate the XDR.
export const activateWalletEndpoint = async (req: any, res: any) => {
  res.status(501).json({ message: "Not implemented yet" });
};
